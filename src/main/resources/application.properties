# ✅ Application Info
spring.application.name=satuhatis

# ✅ Database Config (PostgreSQL)
spring.datasource.url=jdbc:postgresql://ep-silent-star-a8zmqqj7-pooler.eastus2.azure.neon.tech/neondb?sslmode=require
spring.datasource.username=neondb_owner
spring.datasource.password=npg_iJhE19yxnrAT
spring.datasource.driver-class-name=org.postgresql.Driver

# ✅ JPA / Hibernate Config
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.jdbc.time_zone=UTC

# ✅ Thymeleaf Template Engine
spring.thymeleaf.cache=false

# ✅ Logging - Hibernate SQL & Bind Params
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# ✅ Character Encoding - UTF-8 Fix for Form Inputs
server.servlet.encoding.charset=UTF-8
server.servlet.encoding.enabled=true
server.servlet.encoding.force=true

# ✅ Custom Server Port (Uncomment if needed)
# server.port=8080

# ✅ Security - Custom Auth in Use, No In-Memory User
# (No config needed here if pakai login page sendiri)
